import SpriteKit
import SwiftUI

public enum TypeAircraft: Int {
    
    case pilot = 0
    case airplane = 1
    case freighter = 2
    
}

public enum Direction: Int {
    
    case left = 0
    case right = 1
    
}
